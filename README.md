# technicalAssessment

Steps to run the project
- Clone the repository
- Navigate into folder  `technicalAssessment/TopUp`
- Run commands: `docker-compose build` and `docker-compose run database` to run SQL Server database container
- Open each solution in your preferred IDE and run both of them using `sql-local` profile